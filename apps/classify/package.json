{
  "name": "kapi-classify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "W2 kapi-classify — kural tabanlı sınıflandırma + skor (Cloudflare Worker)",
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}